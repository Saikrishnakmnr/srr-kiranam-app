Use the existing `srr-kiranam-app` Supabase project.

Run your existing schema first. Then add the new tables/fields required for offers, delivery zones and editable content from the production SQL you already maintain.

Do not replace the existing database blindly. Back up before migrations.
